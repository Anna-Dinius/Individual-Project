AI Prompt to generate LoC report:

generate a LoC report for each file located in the lib folder and all of its subfolders with the following format (and follow the counting rules as described below):

Total: #
Counting rules: Excludes empty lines and comment-only lines. Inline comments on code lines are still counted.
Files scanned: All files under lib and its subdirectories.

---

- Breakdown:
  - lib/path/to/file.type: # lines
  - ...